{{- if .Values.rfsimGnb.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: gnb-nfapi-pnf-rfsim-configmap
data:
  du.conf: |
{{- range $line := (splitList "\n" (include "rfsim-gnb.duconf" .)) }}
    {{ $line }}
{{- end }}
{{- end }}

