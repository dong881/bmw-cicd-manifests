{{- if .Values.gnbNfapiVnf.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: oai-rfsim-gnb-svc
  labels:
    app: gnb-nfapi-vnf
spec:
  type: ClusterIP
  selector:
    app: gnb-nfapi-vnf
  ports:
    - name: rfsim
      port: 4043
      targetPort: 4043
      protocol: TCP
{{- end }}

